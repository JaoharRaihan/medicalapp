import { AppRegistry } from 'react-native';
import App from './App'; // Adjust the path as needed
import { name as appName } from './app.json';

AppRegistry.registerComponent('AFIP', () => App); // Registering 'AFIP' as your app name
